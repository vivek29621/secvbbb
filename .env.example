# NEXT_PUBLIC_GOOGLE_AI_API_KEY=your-google-ai-api-key
#
# Optional. Set it to enable AI-written executive summaries and the
# "Ask the agent" chat (Gemini 1.5 Flash via Google AI).
# Without it, VulnAgent runs fully on deterministic logic (simulated mode).
# Create a key at https://aistudio.google.com/apikey
